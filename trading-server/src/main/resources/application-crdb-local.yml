spring:
  jpa:
    properties:
      hibernate:
        dialect: io.roach.trading.util.CockroachDBDialect
  datasource:
    url: jdbc:postgresql://localhost:26257/trading?sslmode=disable
    username: root
    password:
    hikari:
      transaction-isolation: TRANSACTION_SERIALIZABLE
